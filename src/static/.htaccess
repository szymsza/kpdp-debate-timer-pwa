<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteBase /stopky
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_URI} ^/stopky
  RewriteRule . /stopky/index.html [L]
</IfModule>

import { FunctionalComponent, h } from 'preact';

const Switch: FunctionalComponent = () => (
  <section>
    <label htmlFor="switch-checkbox">Label</label>
    <input type="checkbox" id="switch-checkbox" />
  </section>
);

export default Switch;
